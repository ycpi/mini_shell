#include <cstdlib>
#include <iostream>
#include <string>
using std::cout;
using std::string;

int main() {
  // cout << "please input";
  // string inputs;
  // while (std::getline(std::cin, inputs)) {
  //   for (size_t i = 0; i < inputs.size(); i++) {
  //   }
  // }
  // string tmp = "tes";
  // string test = "tt";
  // tmp.replace(1, 2, test);
  // std::cout << tmp;
  string tmp;
  tmp = tmp + "fail_test\n";
  cout << tmp;
  exit(EXIT_FAILURE);
}
